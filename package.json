{
  "name": "chordata-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev && nodemon server.js",
    "prod": "cross-env NODE_ENV=production && node server.js"
  },
  "author": "Steven Aherne",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "express-promise-router": "^4.1.1",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
